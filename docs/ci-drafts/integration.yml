name: integration

# Fleshed out in Phase 2 (fixture-site lands) and Phase 7 (orchestrator lands).
# Phase 1 ships this file so that the workflow tree is complete from day one.
# Until then, on every push/PR this is a no-op so it cannot accidentally fail
# the repo's CI status while its dependencies have not yet been built.

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "integration.yml is a placeholder until Phase 2 (fixture site) and Phase 7 (orchestrator) land."
