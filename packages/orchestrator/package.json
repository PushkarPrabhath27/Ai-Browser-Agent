{
  "name": "@eai/orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "The control loop. Owns the policy decision function (`decidePolicy`), all budgets, the task lifecycle state machine, and the approval-wait mechanism. The single failure-handling code path through `decidePolicy`.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --build",
    "test": "vitest run"
  }
}
